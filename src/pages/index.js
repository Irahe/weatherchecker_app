import Login from './login';
import Weather from './weather';

const pages = {
    'login': {
        component: Login
    },
    'weather': {
        component: Weather
    }
}


export default pages;
